// Welcome to your blank canvas!console.log('🎨 Welcome to your new project!'); // Add some sparkle to the pagedocument.addEventListener('DOMContentLoaded',function(){console.log('✨ Page loaded and ready for your creativity!'); // Add a subtle animation to the container const container = document.querySelector('.container'); if (container){container.style.opacity = '0'; container.style.transform = 'translateY(20px)'; setTimeout(() =>{container.style.transition = 'all 0.8s ease-out'; container.style.opacity = '1'; container.style.transform = 'translateY(0)';},100);}// Product page functionality if (window.location.pathname === '/product.html'){const addToCartButton = document.getElementById('addToCart'); if (addToCartButton){addToCartButton.addEventListener('click',addToCart);}}// Cart page functionality if (window.location.pathname === '/cart.html'){displayCartItems(); const checkoutButton = document.getElementById('checkoutButton'); if (checkoutButton){checkoutButton.addEventListener('click',checkout);}}}); // Add to Cart Functionalityfunction addToCart(){const product ={id:'product1',// Assuming a product ID name:'Example Product',price:19.99}; let cart = JSON.parse(localStorage.getItem('cart')) ||[]; const existingItemIndex = cart.findIndex(item => item.id === product.id); if (existingItemIndex !== -1){cart[existingItemIndex].quantity += 1;}else{product.quantity = 1; cart.push(product);}localStorage.setItem('cart',JSON.stringify(cart)); alert('Product added to cart!');}// Display Cart Itemsfunction displayCartItems(){let cart = JSON.parse(localStorage.getItem('cart')) ||[]; const cartItemsContainer = document.getElementById('cartItems'); let total = 0; if (cartItemsContainer){cartItemsContainer.innerHTML = ''; // Clear previous items cart.forEach(item =>{const itemDiv = document.createElement('div'); itemDiv.classList.add('cart-item'); itemDiv.innerHTML = ` <span class="cart-item-name">${item.name}</span> <span>$${item.price.toFixed(2)}</span> <span class="cart-item-quantity">Qty:${item.quantity}</span> `; cartItemsContainer.appendChild(itemDiv); total += item.price * item.quantity;}); // Display the total const totalDiv = document.createElement('div'); totalDiv.innerHTML = `<strong>Total:$${total.toFixed(2)}</strong>`; cartItemsContainer.appendChild(totalDiv);}}// Checkout Functionalityfunction checkout(){// Simulate placing an order alert('Thank you for your order!'); // Clear the cart localStorage.removeItem('cart'); // Redirect to order page window.location.href = 'order.html';}
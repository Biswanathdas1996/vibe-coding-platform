// Welcome to your blank canvas!console.log('🎨 Welcome to your new project!'); // Add some sparkle to the pagedocument.addEventListener('DOMContentLoaded',function(){console.log('✨ Page loaded and ready for your creativity!'); // Add a subtle animation to the container const container = document.querySelector('.container'); if (container){container.style.opacity = '0'; container.style.transform = 'translateY(20px)'; setTimeout(() =>{container.style.transition = 'all 0.8s ease-out'; container.style.opacity = '1'; container.style.transform = 'translateY(0)';},100);}}); // Product Data (Example)const products =[{id:1,name:"Product 1",description:"This is product 1.",price:19.99,image:"https://via.placeholder.com/150"},{id:2,name:"Product 2",description:"This is product 2.",price:29.99,image:"https://via.placeholder.com/150"}]; // Cart Functionalitylet cart = JSON.parse(localStorage.getItem('cart')) ||[]; function updateCartDisplay(){const cartItemsElement = document.getElementById('cart-items'); if (!cartItemsElement) return; cartItemsElement.innerHTML = ''; let total = 0; cart.forEach(item =>{const listItem = document.createElement('li'); listItem.textContent = `${item.name}x ${item.quantity}- $${(item.price * item.quantity).toFixed(2)}`; cartItemsElement.appendChild(listItem); total += item.price * item.quantity;}); document.getElementById('cart-total').textContent = total.toFixed(2); localStorage.setItem('cart',JSON.stringify(cart));}function addToCart(productId){const product = products.find(p => p.id === productId); if (!product) return; let cartItem = cart.find(item => item.id === productId); if (cartItem){cartItem.quantity++;}else{cart.push({...product,quantity:1});}updateCartDisplay();}function renderProductPage(){const productContainer = document.querySelector('.container main'); if (!productContainer) return; productContainer.innerHTML = ''; products.forEach(product =>{const productCard = document.createElement('div'); productCard.classList.add('product-card'); productCard.innerHTML = ` <img src="${product.image}" alt="${product.name}"> <h3>${product.name}</h3> <p>${product.description}</p> <p>$${product.price.toFixed(2)}</p> <button data-product-id="${product.id}">Add to Cart</button> `; productContainer.appendChild(productCard);}); // Add event listeners for add to cart buttons after rendering document.querySelectorAll('.product-card button').forEach(button =>{button.addEventListener('click',function(){const productId = parseInt(this.dataset.productId); addToCart(productId);});});}function renderCartPage(){const cartContainer = document.querySelector('.container main'); if (!cartContainer) return; cartContainer.innerHTML = ` <h2>Your Cart</h2> <ul id="cart-items"></ul> <p>Total:$<span id="cart-total">0.00</span></p> `; updateCartDisplay();}function renderOrderPage(){const orderContainer = document.querySelector('.container main'); if (!orderContainer) return; let orderHtml = ` <h2>Order Summary</h2> <ul id="order-items"></ul> <p>Total:$<span id="order-total">0.00</span></p> <button id="place-order-button">Place Order</button> `; orderContainer.innerHTML = orderHtml; const orderItemsElement = document.getElementById('order-items'); const orderTotalElement = document.getElementById('order-total'); let total = 0; cart.forEach(item =>{const listItem = document.createElement('li'); listItem.textContent = `${item.name}x ${item.quantity}- $${(item.price * item.quantity).toFixed(2)}`; orderItemsElement.appendChild(listItem); total += item.price * item.quantity;}); orderTotalElement.textContent = total.toFixed(2); document.getElementById('place-order-button').addEventListener('click',function(){alert('Order placed! (Placeholder - no backend processing)'); cart =[]; // Clear cart localStorage.removeItem('cart'); updateCartDisplay(); renderOrderPage(); // Re-render the order page});}// Routing (very basic - for this example) document.addEventListener('DOMContentLoaded',() =>{// Default to home page (can be extended with more complex routing) let currentPage = window.location.pathname; if (currentPage.endsWith('product.html')){renderProductPage();}else if (currentPage.endsWith('cart.html')){renderCartPage();}else if (currentPage.endsWith('order.html')){renderOrderPage();}});